import { StyleSheet } from "react-native";
export const stylesGlobal = StyleSheet.create({
    title: {
      fontSize: 24,
      fontWeight: "bold",
      textAlign: "left"
    },
    text:{
      fontSize: 20,
      fontWeight: "bold",
    }
  }); 

import { StyleSheet } from "react-native";
export const stylesGlobal = StyleSheet.create({
    title: {
      fontSize: 20,
      fontWeight: "bold",
      textAlign: "left"
    },
    text:{
      fontSize: 16,
      fontWeight: "bold",
    }
  }); 
